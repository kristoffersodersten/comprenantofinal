import SwiftUI

/// A placeholder view for the transcription feature.
struct TranscriptionView: View {
    var body: some View {
        Text("Transcription Feature Coming Soon")
            .font(.title)
            .padding()
    }
}

/// Preview for the TranscriptionView.
struct TranscriptionView_Previews: PreviewProvider {
    static var previews: some View {
        TranscriptionView()
    }
}
